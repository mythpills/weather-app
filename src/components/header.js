import React from "react";

const Header = () => {
  return (
    <>
      <header className="App-header">Catrancher</header>
      <div>{new Date()}</div>
    </>
  );
};

export default Header;
